This folder exists to match Render services configured with Root Directory = `backend`.

If your Render service points to this folder:
- Build Command: `./build.sh`
- Start Command: `./start.sh`

The scripts call the real Django project from the repository root.
